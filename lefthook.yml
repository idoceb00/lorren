pre-commit:
  parallel: true
  commands:
    fmt:
      glob: "*.go"
      run: test -z "$(gofmt -l .)"
    vet:
      glob: "*.go"
      run: go vet ./...

pre-push:
  commands:
    test:
      run: go test ./...